<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:viewer.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="400"
        x:Class="viewer.Views.MainWindow"
        x:DataType="vm:MainWindowViewModel"
        Icon="avares://viewer/Assets/vnc-viewer.png"
        Title="VNC Viewer"
        MinWidth="320"
        MinHeight="480"
        ExtendClientAreaToDecorationsHint="True"
        ExtendClientAreaChromeHints="NoChrome"
        Background="Transparent"
        SystemDecorations="Full">

        <!--
            
            TransparencyLevelHint="Transparent"
            ExtendClientAreaToDecorationsHint="True"
            ExtendClientAreaChromeHints="NoChrome"
            SystemDecorations="BorderOnly"
            CanResize="True"
        -->

    <Design.DataContext>
        <!-- This only sets the DataContext for the previewer in an IDE,
             to set the actual DataContext for runtime, set the DataContext property in code (look at App.axaml.cs) -->
        <vm:MainWindowViewModel/>
    </Design.DataContext>

    <Border Classes="window">
        <Grid RowDefinitions="Auto, *">
            <Grid Grid.Row="0">
                <DockPanel Name="WindowDragHandle" PointerMoved="WindowDragHandle_OnPointerMoved"
                    PointerPressed="WindowDragHandle_OnPointerPressed" PointerReleased="WindowDragHandle_OnPointerReleased"/>
                <DockPanel Name="Titlebar">
                    <StackPanel Name="TitlebarButtons">
                        <Button Name="CloseButton"><Path/></Button>
                        <Button Name="MinimizeButton"><Path/></Button>
                        <Button Name="ZoomButton"><Path/></Button>
                    </StackPanel>
                    <StackPanel Name="TitleIcon">
                        <Image Source="/Assets/vnc-viewer.png"/>
                        <TextBlock Text="VNC Multicast"/>
                    </StackPanel>
                </DockPanel>
            </Grid>
            <Grid RowDefinitions="Auto, *" Classes="body" Grid.Row="1">
                <Border Classes="header" Grid.Row="0">
                    <StackPanel Classes="title">
                        <TextBlock Classes="title special" Text="{Binding AppTitleFirst}"/>
                        <TextBlock Classes="title" Text="{Binding AppTitleSecond}"/>
                    </StackPanel>
                </Border>
                <Grid ColumnDefinitions="1*, 2*, 1*" RowDefinitions="*, Auto" Grid.Row="1">
                    <ListBox Name="SessionPicker" Classes="session-picker" Grid.Row="0" Grid.Column="1" SelectionMode="Toggle">
                    </ListBox>
                    <Button Classes="select" Content="SELECT" Grid.Row="1" Grid.Column="1"/>
                </Grid>
                <Grid RowDefinitions="Auto, *" Classes="body" Grid.Row="1" IsVisible="False">
                    <Grid ColumnDefinitions="1*, 2*, 1*" RowDefinitions="*, Auto">
                        <ListBox Name="SessionPicker2" Classes="session-picker" Grid.Row="0" Grid.Column="1" SelectionMode="Toggle">
                        </ListBox>
                        <Button Classes="select" Content="SELECT" Grid.Row="1" Grid.Column="1"/>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>
    </Border>

    <!--StackPanel>
        <Grid RowDefinitions="Auto, *" Classes="mainContainer">
            <Border Classes="header" Grid.Row="0">
                <StackPanel Classes="title">
                    <TextBlock Classes="title"> MO </TextBlock>
                    <TextBlock Classes="titleYellow"> VIEW </TextBlock>
                    <TextBlock Classes="title"> ER </TextBlock>
                    <Image/>
                </StackPanel>
            </Border>
            <Border Classes="header" Grid.Row="1">
                <Button Classes="header">Calculate</Button>
            </Border>
        </Grid>

        <Grid Margin="5"
            ColumnDefinitions="120, 100" 
            RowDefinitions="Auto, Auto, Auto">
            <Label Grid.Row="0" Grid.Column="0" Margin="10">Celsius</Label>
            <TextBox Grid.Row="0" Grid.Column="1" Margin="0 5" Text="0" Name="celsius"/>
            <Label Grid.Row="1" Grid.Column="0" Margin="10">Fahrenheit</Label>
            <TextBox Grid.Row="1"  Grid.Column="1" Margin="0 5" Text="0" Name="fahrenheit" IsReadOnly="True"/>
            <Button Grid.Row="2" Grid.Column="1"
                Margin="0 5" Click="Button_OnClick">
                Calculate
            </Button>
        </Grid>
    </StackPanel!-->
</Window>
